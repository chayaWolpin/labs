use debug::PrintTrait;
fn main(){
    let is_awesome=true;
    let version:u8=2;
    if is_awesome&&version>0{
        'Lets code!'.print();
    }
    else{
        'great things are coming..'.print();
    }
}
